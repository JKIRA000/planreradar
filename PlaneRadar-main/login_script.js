//document.getElementById('loginForm').addEventListener('submit', function(event) {
    //ev/ent.preventDefault(); // Prevent form submission

    //const username = document.getElementById('username').value;
    //const password = document.getElementById('password').value;

    // Basic validation
    //if (username === "admin" && password === "password123") {
        //alert("Login successful!");
      //  window.location.href = "main.html"; // Redirect to the main page
    //} else {
    //    alert("Invalid username or password. Please try again.");
  //  }
//});

//docume//nt.getElementById('loginForm').addEventListener('submit', function(event) {
    //ev/ent.preventDefault(); // Prevent form submission

    //const username = document.getElementById('username').value;
    //const password = document.getElementById('password').value;

    // Basic validation
    //if (username === "admin" && password === "password123") {
        //alert("Login successful!");
      //  window.location.href = "main.html"; // Redirect to the main page
    //} else {
    //    alert("Invalid username or password. Please try again.");
    //}
//});

// Toggle password visibility
//const togglePassword = document.getElementById('togglePassword');
//const passwordInput = document.getElementById('password');

//togglePassword.addEventListener('click', function() {
    //const type = passwordInput.getAttribute('type') === 'password' ? 'text' : 'password';
    //passwordInput.setAttribute('type', type);
  //  this.textContent = type === 'password' ? '👁️' : '🙈'; // Change icon when toggled
//});

//document.getElementById('loginForm').addEventListener('submit', function(event) {
    //event.preventDefault(); // Prevent form submission
    
    //const username = document.getElementById('loginUsername').value;
    //const password = document.getElementById('loginPassword').value;

    // Retrieve user data from localStorage
    //const storedUsername = localStorage.getItem('username');
    //const storedPassword = localStorage.getItem('password');

    // Log values to check
    //console.log('Entered Username:', username);
    //console.log('Entered Password:', password);
//    console.log('Stored Username:', storedUsername);
    //console.log('Stored Password:', storedPassword);

    // Check if credentials match
    //if (username === storedUsername && password === storedPassword) {
      //  alert("Login successful!");
        // Redirect to the main page or dashboard
    //    window.location.href = "dashboard.html"; // Change this to your main page
    //} else {
    //    alert("Invalid username or password. Please try again.");
  //  }
//});
